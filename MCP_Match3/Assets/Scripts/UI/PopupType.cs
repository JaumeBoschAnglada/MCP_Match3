namespace Match3.UI
{
    /// <summary>
    /// Enum for all available popup types.
    /// Used to identify which popup to open without direct prefab references in buttons/triggers.
    /// </summary>
    public enum PopupType
    {
        Pause,
        // Add more popup types here as needed (Shop, Settings, GameOver, etc.)
    }
}
